{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capabilities for the LiveTranscript desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
