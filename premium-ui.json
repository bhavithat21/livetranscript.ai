{
  "profile": "product-admin",
  "canonicalMap": "UX-CONTRACT.md",
  "scope": "Remote assistance and app identity feature; unrelated existing product screens are outside this audit.",
  "sourceRoots": ["components/remote", "lib/appIdentity", "app/(app)/remote", "app/(app)/settings"],
  "requiredCapabilities": ["Form", "Scrollbar", "Toast"],
  "ownership": {},
  "requiredCommands": ["lint", "typecheck", "test", "build"],
  "commands": {
    "lint": "pnpm lint",
    "typecheck": "pnpm exec tsc --noEmit",
    "test": "pnpm test",
    "build": "pnpm build"
  }
}
